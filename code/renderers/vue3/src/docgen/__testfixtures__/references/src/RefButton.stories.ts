// Fixture read from disk by the project-manager tests, never imported.
import RefButton from './RefButton.vue';

export default {
  title: 'Example/RefButton',
  component: RefButton,
};

export const Default = { args: { label: 'Hello' } };
